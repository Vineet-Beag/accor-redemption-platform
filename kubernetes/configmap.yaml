apiVersion: v1
kind: ConfigMap

metadata:
  name: redemption-config
  namespace: redemption

data:

  ENVIRONMENT: "production"

  LOG_LEVEL: "INFO"

  REGION: "APAC"

  FEATURE_REDEMPTION_ENABLED: "true"
